[package]
name = "agentwallet"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "2.1"
borsh = "1.5"
spl-token = { version = "7", features = ["no-entrypoint"] }

[features]
no-entrypoint = []
